diff --git a/ZAPDTR/ZAPD/CrashHandler.cpp b/ZAPDTR/ZAPD/CrashHandler.cpp
index 17aa9d5..3d0c4ce 100644
--- a/ZAPDTR/ZAPD/CrashHandler.cpp
+++ b/ZAPDTR/ZAPD/CrashHandler.cpp
@@ -1,7 +1,7 @@
 #include "CrashHandler.h"
 #include <ship/utils/StringHelper.h>
 
-#if __has_include(<unistd.h>)
+#if __has_include(<unistd.h>) && !defined(__ANDROID__)
 #define HAS_POSIX 1
 #else
 #define HAS_POSIX 0
diff --git a/combo/CMakeLists.txt b/combo/CMakeLists.txt
index 9a7387b..1936cdd 100644
--- a/combo/CMakeLists.txt
+++ b/combo/CMakeLists.txt
@@ -168,7 +168,7 @@ if(CMAKE_SYSTEM_NAME STREQUAL "Windows")
         target_link_options(ComboShip PRIVATE
             $<$<CONFIG:Release>:/SUBSYSTEM:WINDOWS;/ENTRY:mainCRTStartup>)
     endif()
-else()
+elseif(NOT ANDROID)
     # ComboShip Linux/macOS: the games' shared objects land in their own build dirs; mirror the
     # Windows single-deploy-dir layout by symlinking them beside the launcher, whose loader dlopens
     # ./libsoh.so etc. from its own directory (cwd == exe dir at runtime). Symlinks, not copies:
@@ -196,6 +196,7 @@ endif()
     # fails on ("Raw data size: 0x0", reached EOF). Clearing makes the source tree authoritative each
     # build. (The user's ROM/port archives live in the dir root, NOT under assets/, so this is safe.)
     # See docs/merges/2026-06-20.md.
+    if(NOT ANDROID)
     add_custom_command(TARGET ComboShip POST_BUILD
         COMMAND ${CMAKE_COMMAND} -E rm -rf $<TARGET_FILE_DIR:ComboShip>/assets
         COMMAND ${CMAKE_COMMAND} -E copy_directory
@@ -233,5 +234,7 @@ endif()
         message(STATUS "ComboShip: mm/2ship.o2r not found — skipping .o2r deploy (run Generate2ShipOtr to produce it).")
     endif()
 
+    endif() # NOT ANDROID: Android assets are staged by stage_runtime.py.
+
 # Install
 install(TARGETS ComboShip DESTINATION . COMPONENT combo)
diff --git a/combo/ComboShip.cpp b/combo/ComboShip.cpp
index a7f0392..6e0a1da 100644
--- a/combo/ComboShip.cpp
+++ b/combo/ComboShip.cpp
@@ -2828,6 +2828,7 @@ static int LauncherValidateShipConfig(const char* path) {
 // ---------- Entry point ----------
 
 #ifdef __ANDROID__
+extern void ComboAndroid_SetGameplayActive(bool active);
 int ComboAndroid_RunDesktopMain(int argc, char** argv) {
 #else
 int main(int argc, char** argv) {
@@ -3121,7 +3122,11 @@ int main(int argc, char** argv) {
                 FreeDll(comboUIModule);
             FreeDll(mmModule);
             FreeDll(sohModule);
+#ifdef __ANDROID__
+            return 0; // Return to Android setup after the user dismisses the extraction screen.
+#else
             return 1;
+#endif
         }
         if (!OOTArchivesExist() || !MMRomArchiveExists()) {
             std::cerr << "ERROR: ROM archives still missing after extraction — exiting." << std::endl;
@@ -3417,6 +3422,9 @@ int main(int argc, char** argv) {
     // MM was already booted at startup (eager boot) — the first portal transition must RESUME MM,
     // not run MM_RunGame (which would re-run MM_RunMain on an already-initialized MM).
     bool mmBooted = mmEagerBooted;
+#ifdef __ANDROID__
+    ComboAndroid_SetGameplayActive(true);
+#endif
     for (;;) {
         if (current == GAME_OOT) {
             g_PendingMMFileNum = -1;
@@ -3482,6 +3490,9 @@ int main(int argc, char** argv) {
         }
     }
 
+#ifdef __ANDROID__
+    ComboAndroid_SetGameplayActive(false);
+#endif
     // Teardown order is load-bearing: MM before SOH (MM holds a shared_ptr to the SHARED Context;
     // SOH's DeinitOTR releases the last ref, running ~Context here — saves geometry/config, destroys
     // the window). All thread-owners must be joined before the FreeDll calls (joining under the loader
diff --git a/combo/gui/ComboExtractScreen.cpp b/combo/gui/ComboExtractScreen.cpp
index 33f2c76..53c543e 100644
--- a/combo/gui/ComboExtractScreen.cpp
+++ b/combo/gui/ComboExtractScreen.cpp
@@ -192,8 +192,12 @@ extern "C" COMBO_EXPORT int ComboUI_RunExtraction(const ComboExtractCallbacks* c
         // (a new player's window can launch small/square before any resolution is persisted).
         const float kMargin = 20.0f;
         float panelW = vp->WorkSize.x - kMargin * 2.0f;
-        if (panelW > 680.0f) {
-            panelW = 680.0f;
+        float preferredW = 680.0f;
+#ifdef __ANDROID__
+        preferredW *= ImGui::GetIO().FontGlobalScale;
+#endif
+        if (panelW > preferredW) {
+            panelW = preferredW;
         }
         float panelMaxH = vp->WorkSize.y - kMargin * 2.0f;
         ImGui::SetNextWindowPos(vp->GetCenter(), ImGuiCond_Always, ImVec2(0.5f, 0.5f));
@@ -248,6 +252,14 @@ extern "C" COMBO_EXPORT int ComboUI_RunExtraction(const ComboExtractCallbacks* c
                     }
                 }
 
+#ifdef __ANDROID__
+                ImGui::TextWrapped("Your imported Ocarina of Time and Majora's Mask ROMs are checked automatically. "
+                                   "Choose Extract when both are valid.");
+                if (!slots[0].valid || !slots[1].valid) {
+                    ImGui::TextWrapped("If an imported ROM is missing or unsupported, choose Quit to return to "
+                                       "setup, import a supported copy, and reopen the game.");
+                }
+#else
                 ImGui::TextWrapped("ComboShip needs both an Ocarina of Time ROM and a Majora's Mask ROM. "
                                    "Select each ROM below or drag and drop ROM files onto this window, "
                                    "then click Extract. Both are required.");
@@ -259,6 +271,7 @@ extern "C" COMBO_EXPORT int ComboUI_RunExtraction(const ComboExtractCallbacks* c
                                        "drag and drop your ROM files onto this window instead.");
                     ImGui::PopStyleColor();
                 }
+#endif
                 ImGui::Spacing();
 
                 for (int i = 0; i < 2; i++) {
@@ -270,6 +283,7 @@ extern "C" COMBO_EXPORT int ComboUI_RunExtraction(const ComboExtractCallbacks* c
                         ImGui::TextColored(kGreen, "(already extracted)");
                     } else {
                         ImGui::TextWrapped("%s", s.path.empty() ? "(no ROM selected)" : s.path.c_str());
+#ifndef __ANDROID__
                         ComboMenu_PushButton(theme);
                         if (!PickerAvailable()) {
                             ImGui::BeginDisabled();
@@ -286,6 +300,7 @@ extern "C" COMBO_EXPORT int ComboUI_RunExtraction(const ComboExtractCallbacks* c
                         }
                         ComboMenu_PopButton();
                         ImGui::SameLine();
+#endif
                         if (s.path.empty()) {
                             ImGui::TextDisabled("required");
                         } else if (s.valid) {
diff --git a/libultraship/include/ship/port/mobile/MobileImpl.h b/libultraship/include/ship/port/mobile/MobileImpl.h
index de38dd2..2039bb0 100644
--- a/libultraship/include/ship/port/mobile/MobileImpl.h
+++ b/libultraship/include/ship/port/mobile/MobileImpl.h
@@ -10,5 +10,13 @@ namespace Ship {
 class Mobile {
   public:
     static void ImGuiProcessEvent(bool wantsTextInput);
+#ifdef __ANDROID__
+    static void RequestMenu();
+    static bool ConsumeMenuRequest();
+    static void UpdateMenuState(bool visible);
+    static bool IsMenuVisible();
+    static void RequestMenuScale(float scale);
+    static void ApplyMenuScale();
+#endif
 };
 }; // namespace Ship
diff --git a/libultraship/src/ship/port/mobile/MobileImpl.cpp b/libultraship/src/ship/port/mobile/MobileImpl.cpp
index 6fbd4a0..acffb8e 100644
--- a/libultraship/src/ship/port/mobile/MobileImpl.cpp
+++ b/libultraship/src/ship/port/mobile/MobileImpl.cpp
@@ -3,6 +3,42 @@
 #include <SDL2/SDL.h>
 
 #include <imgui_internal.h>
+#ifdef __ANDROID__
+#include <atomic>
+#include <algorithm>
+#include <cmath>
+#include "libultraship/bridge/consolevariablebridge.h"
+#include "ship/Context.h"
+#include "ship/window/Window.h"
+#include "ship/window/gui/Gui.h"
+
+namespace {
+std::atomic<bool> menuRequested{false}, menuVisible{true};
+std::atomic<float> requestedScale{0.0f};
+}
+void Ship::Mobile::RequestMenu() { menuRequested = true; }
+bool Ship::Mobile::ConsumeMenuRequest() { return menuRequested.exchange(false); }
+void Ship::Mobile::UpdateMenuState(bool visible) { menuVisible = visible; }
+bool Ship::Mobile::IsMenuVisible() { return menuVisible.load(); }
+void Ship::Mobile::RequestMenuScale(float scale) {
+    if (std::isfinite(scale)) requestedScale = std::clamp(scale, 1.0f, 3.0f);
+}
+void Ship::Mobile::ApplyMenuScale() {
+    const float requested = requestedScale.exchange(0.0f);
+    if (requested > 0.0f) {
+        CVarSetFloat("gSettings.Menu.AndroidScale", requested);
+        Ship::Context::GetRawInstance()->GetWindow()->GetGui()->SaveConsoleVariablesNextFrame();
+    }
+    const float scale = std::clamp(CVarGetFloat("gSettings.Menu.AndroidScale", 1.45f), 1.0f, 3.0f);
+    ImGuiStyle& style = ImGui::GetStyle();
+    style.FramePadding = ImVec2(4.0f * scale, 6.0f * scale);
+    style.ItemSpacing = ImVec2(8.0f * scale, 6.0f * scale);
+    style.ItemInnerSpacing = ImVec2(4.0f * scale, 4.0f * scale);
+    style.ScrollbarSize = 14.0f * scale;
+    style.GrabMinSize = 12.0f * scale;
+    ImGui::GetIO().FontGlobalScale = scale;
+}
+#endif
 
 static bool isShowingVirtualKeyboard = true;
 
diff --git a/libultraship/src/ship/window/gui/Gui.cpp b/libultraship/src/ship/window/gui/Gui.cpp
index 4f89979..a7ff1fe 100644
--- a/libultraship/src/ship/window/gui/Gui.cpp
+++ b/libultraship/src/ship/window/gui/Gui.cpp
@@ -15,6 +15,9 @@
 #include "ship/window/gui/Fonts.h"
 #include "ship/window/gui/resource/GuiTextureFactory.h"
 #include "ship/window/gui/resource/GuiTexture.h"
+#ifdef __ANDROID__
+#include "ship/port/mobile/MobileImpl.h"
+#endif
 
 namespace Ship {
 #define TOGGLE_BTN ImGuiKey_F1
@@ -244,12 +247,18 @@ void Gui::DrawMenu() {
 
     ImGui::DockSpace(dockId, ImVec2(0.0f, 0.0f), ImGuiDockNodeFlags_None | ImGuiDockNodeFlags_NoDockingInCentralNode);
 
-    if (ImGui::IsKeyPressed(TOGGLE_BTN, false) || ImGui::IsKeyPressed(ImGuiKey_Escape, false) ||
+    const bool androidMenuRequest =
+#ifdef __ANDROID__
+        Mobile::ConsumeMenuRequest();
+#else
+        false;
+#endif
+    if (androidMenuRequest || ImGui::IsKeyPressed(TOGGLE_BTN, false) || ImGui::IsKeyPressed(ImGuiKey_Escape, false) ||
         (ImGui::IsKeyPressed(TOGGLE_PAD_BTN, false) &&
          Ship::Context::GetRawInstance()->GetConsoleVariables()->GetInteger(CVAR_IMGUI_CONTROLLER_NAV, 0))) {
-        if ((ImGui::IsKeyPressed(ImGuiKey_Escape, false) || ImGui::IsKeyPressed(TOGGLE_PAD_BTN, false)) && GetMenu()) {
+        if ((androidMenuRequest || ImGui::IsKeyPressed(ImGuiKey_Escape, false) || ImGui::IsKeyPressed(TOGGLE_PAD_BTN, false)) && GetMenu()) {
             GetMenu()->ToggleVisibility();
-        } else if ((ImGui::IsKeyPressed(TOGGLE_BTN, false) || ImGui::IsKeyPressed(TOGGLE_PAD_BTN, false)) &&
+        } else if ((androidMenuRequest || ImGui::IsKeyPressed(TOGGLE_BTN, false) || ImGui::IsKeyPressed(TOGGLE_PAD_BTN, false)) &&
                    GetMenuBar()) {
             GetMenuBar()->ToggleVisibility();
         }
@@ -285,6 +294,9 @@ void Gui::DrawMenu() {
         windowIter.second->Draw();
     }
 
+#ifdef __ANDROID__
+    Mobile::UpdateMenuState(GetMenuOrMenubarVisible() || ImGui::IsPopupOpen(nullptr, ImGuiPopupFlags_AnyPopupId | ImGuiPopupFlags_AnyPopupLevel));
+#endif
     ImGui::End();
 }
 
@@ -306,6 +318,9 @@ void Gui::StartFrame() {
     HandleMouseCapture();
     ImGuiBackendNewFrame();
     ImGuiWMNewFrame();
+#ifdef __ANDROID__
+    Mobile::ApplyMenuScale();
+#endif
     ImGui::NewFrame();
 }
 
diff --git a/mm/2s2h/Enhancements/ModMenu/ModMenu.cpp b/mm/2s2h/Enhancements/ModMenu/ModMenu.cpp
index 95a057c..1fc9d30 100644
--- a/mm/2s2h/Enhancements/ModMenu/ModMenu.cpp
+++ b/mm/2s2h/Enhancements/ModMenu/ModMenu.cpp
@@ -1,4 +1,5 @@
 #include <map>
+#include <cstdlib>
 #include <vector>
 
 #include <libultraship/classes.h>
@@ -131,6 +132,10 @@ bool IsValidExtension(std::string extension) {
 static bool archivesAdded = false;
 
 void UpdateModFiles(bool init = false, bool reset = false) {
+#ifdef __ANDROID__
+    const char* skipMods = std::getenv("COMBOSHIP_SKIP_MODS");
+    if (skipMods && skipMods[0] == '1') return;
+#endif
     if (init || reset) {
         enabledModFiles.clear();
         enabledModFiles = GetEnabledModsFromCVar();
diff --git a/mm/src/audio/lib/load.c b/mm/src/audio/lib/load.c
index 6e3b1b9..54ffddb 100644
--- a/mm/src/audio/lib/load.c
+++ b/mm/src/audio/lib/load.c
@@ -850,7 +850,7 @@ void* AudioLoad_SyncLoad(s32 tableType, u32 id, s32* didAllocate) {
                 size -= 0x10;
             }
 
-            memcpy(ramAddr, romAddr, size);
+            memcpy((void*)ramAddr, (const void*)romAddr, size);
         } else if (medium2 == mediumUnk) {
             AudioLoad_SyncDmaUnkMedium(romAddr, ramAddr, size, (s16)table->unkMediumParam);
         } else {
@@ -976,7 +976,7 @@ s32 AudioLoad_Dma(OSIoMesg* mesg, u32 priority, s32 direction, uintptr_t devAddr
     if (gAudioCtx.resetTimer > 0x10) {
         return -1;
     }
-    memcpy(ramAddr, devAddr, size);
+    memcpy((void*)ramAddr, (const void*)devAddr, size);
     return 0;
 }
 
@@ -1791,7 +1791,7 @@ void AudioLoad_AsyncDmaRamUnloaded(AudioAsyncLoad* asyncLoad, size_t size) {
     size = ALIGN16(size);
     Audio_InvalDCache(asyncLoad->curRamAddr, size);
     osCreateMesgQueue(&asyncLoad->msgQueue, &asyncLoad->msg, 1);
-    memcpy(asyncLoad->curRamAddr, asyncLoad->curDevAddr, size);
+    memcpy((void*)asyncLoad->curRamAddr, (const void*)asyncLoad->curDevAddr, size);
     osSendMesg(&asyncLoad->msgQueue, OS_MESG_PTR(NULL), OS_MESG_NOBLOCK);
 }
 
diff --git a/mm/src/boot/O2/loadfragment.c b/mm/src/boot/O2/loadfragment.c
index a933de3..b8f6672 100644
--- a/mm/src/boot/O2/loadfragment.c
+++ b/mm/src/boot/O2/loadfragment.c
@@ -193,7 +193,7 @@ size_t Fragment_Load(uintptr_t vromStart, uintptr_t vromEnd, void* vramStart, vo
             // Clear BSS area (%08x-%08x)
             // "BSS領域をクリアします(%08x-%08x)\n"
         }
-        memset(end, 0, ovlRelocs->bssSize);
+        memset((void*)end, 0, ovlRelocs->bssSize);
     }
 
     osWritebackDCache(allocatedRamAddr, allocatedBytes);
@@ -271,7 +271,7 @@ void* Fragment_AllocateAndLoad(uintptr_t vromStart, uintptr_t vromEnd, void* vra
             // Clear BSS area (%08x-%08x)
             // "BSS領域をクリアします(%08x-%08x)\n"
         }
-        memset(end, 0, ovlRelocs->bssSize);
+        memset((void*)end, 0, ovlRelocs->bssSize);
     }
 
     osInvalICache(allocatedRamAddr, allocatedBytes);
diff --git a/mm/src/code/title_setup.c b/mm/src/code/title_setup.c
index faeb437..4d23e8d 100644
--- a/mm/src/code/title_setup.c
+++ b/mm/src/code/title_setup.c
@@ -1,6 +1,7 @@
 #include "z_title_setup.h"
 #include "overlays/gamestates/ovl_title/z_title.h"
 #ifdef COMBO_BUILD
+#include <libultraship/bridge/consolevariablebridge.h>
 #include "global.h"
 #include "BenPort.h"
 #include "2s2h/GameInteractor/GameInteractor.h"
diff --git a/mm/src/code/z_skelanime.c b/mm/src/code/z_skelanime.c
index 0f45cb1..6191fd1 100644
--- a/mm/src/code/z_skelanime.c
+++ b/mm/src/code/z_skelanime.c
@@ -1067,7 +1067,7 @@ void AnimTaskQueue_AddLoadPlayerFrame(PlayState* play, PlayerAnimationHeader* an
         if (animData == NULL) {
             return;
         }
-        memcpy(ram, (uintptr_t)animData + (((sizeof(Vec3s) * limbCount + 2) * frame)), sizeof(Vec3s) * limbCount + 2);
+        memcpy(ram, (const u8*)animData + (((sizeof(Vec3s) * limbCount + 2) * frame)), sizeof(Vec3s) * limbCount + 2);
     }
 }
 
diff --git a/soh/include/functions.h b/soh/include/functions.h
index b74aaa1..e372122 100644
--- a/soh/include/functions.h
+++ b/soh/include/functions.h
@@ -61,9 +61,9 @@ u32 func_80001F8C(void);
 u32 Locale_IsRegionNative(void);
 #ifdef __WIIU__
 void _assert(const char* exp, const char* file, s32 line);
-#elif defined(__linux__)
+#elif defined(__linux__) && !defined(__ANDROID__)
 void __assert(const char* exp, const char* file, s32 line) __THROW;
-#elif !defined(__APPLE__) && !defined(__SWITCH__) && !defined(__OpenBSD__)
+#elif !defined(__APPLE__) && !defined(__SWITCH__) && !defined(__OpenBSD__) && !defined(__ANDROID__)
 void __assert(const char* exp, const char* file, s32 line);
 #endif
 #if defined(__APPLE__) && defined(NDEBUG)
diff --git a/soh/soh/Enhancements/mod_menu.cpp b/soh/soh/Enhancements/mod_menu.cpp
index 25f28a0..2ed1b01 100644
--- a/soh/soh/Enhancements/mod_menu.cpp
+++ b/soh/soh/Enhancements/mod_menu.cpp
@@ -1,5 +1,6 @@
 #include <algorithm>
 #include <cctype>
+#include <cstdlib>
 #include <map>
 #include <set>
 #include <vector>
@@ -201,6 +202,10 @@ bool IsValidExtension(std::string extension) {
 }
 
 void UpdateModFiles(bool init = false, bool reset = false) {
+#ifdef __ANDROID__
+    const char* skipMods = std::getenv("COMBOSHIP_SKIP_MODS");
+    if (skipMods && skipMods[0] == '1') return;
+#endif
     if (init || reset) {
         enabledModFiles.clear();
         enabledModFiles = GetEnabledModsFromCVar();
diff --git a/soh/src/code/audio_load.c b/soh/src/code/audio_load.c
index fcebb71..b1111a5 100644
--- a/soh/src/code/audio_load.c
+++ b/soh/src/code/audio_load.c
@@ -987,7 +987,7 @@ s32 AudioLoad_Dma(OSIoMesg* mesg, u32 priority, s32 direction, uintptr_t devAddr
         return -1;
     }
 
-    memcpy(ramAddr, devAddr, size);
+    memcpy((void*)ramAddr, (const void*)devAddr, size);
 
     return 0;
 }
diff --git a/soh/src/code/z_message_PAL.c b/soh/src/code/z_message_PAL.c
index 881896a..42d6e78 100644
--- a/soh/src/code/z_message_PAL.c
+++ b/soh/src/code/z_message_PAL.c
@@ -1622,7 +1622,7 @@ void Message_LoadItemIcon(PlayState* play, u16 itemId, s16 y) {
         R_TEXTBOX_ICON_XPOS = R_TEXT_INIT_XPOS - sIconItem32XOffsets[language];
         R_TEXTBOX_ICON_YPOS = y + 6;
         R_TEXTBOX_ICON_SIZE = 32;
-        memcpy((uintptr_t)msgCtx->textboxSegment + MESSAGE_STATIC_TEX_SIZE, gItemIcons[itemId],
+        memcpy((u8*)msgCtx->textboxSegment + MESSAGE_STATIC_TEX_SIZE, gItemIcons[itemId],
                strlen(gItemIcons[itemId]) + 1);
         // "Item 32-0"
         osSyncPrintf("アイテム32-0\n");
@@ -1630,7 +1630,7 @@ void Message_LoadItemIcon(PlayState* play, u16 itemId, s16 y) {
         R_TEXTBOX_ICON_XPOS = R_TEXT_INIT_XPOS - sIconItem24XOffsets[language];
         R_TEXTBOX_ICON_YPOS = y + 10;
         R_TEXTBOX_ICON_SIZE = 24;
-        memcpy((uintptr_t)msgCtx->textboxSegment + MESSAGE_STATIC_TEX_SIZE, gItemIcons[itemId],
+        memcpy((u8*)msgCtx->textboxSegment + MESSAGE_STATIC_TEX_SIZE, gItemIcons[itemId],
                strlen(gItemIcons[itemId]) + 1);
         // "Item 24"
         osSyncPrintf("アイテム24＝%d (%d) {%d}\n", itemId, itemId - ITEM_KOKIRI_EMERALD, 84);
@@ -2202,9 +2202,9 @@ void Message_DecodeJPN(PlayState* play) {
             msgCtx->textboxBackgroundBackColorIdx = (font->msgBufWide[msgCtx->msgBufPos + 2] & 0xF00) >> 8;
             msgCtx->textboxBackgroundYOffsetIdx = (font->msgBufWide[msgCtx->msgBufPos + 2] & 0xF0) >> 4;
             msgCtx->textboxBackgroundUnkArg = font->msgBufWide[msgCtx->msgBufPos + 2] & 0xF;
-            memcpy((uintptr_t)msgCtx->textboxSegment + MESSAGE_STATIC_TEX_SIZE, gRedMessageXLeftTex,
+            memcpy((u8*)msgCtx->textboxSegment + MESSAGE_STATIC_TEX_SIZE, gRedMessageXLeftTex,
                    strlen(gRedMessageXLeftTex) + 1);
-            memcpy((uintptr_t)msgCtx->textboxSegment + MESSAGE_STATIC_TEX_SIZE + 0x900, gRedMessageXRightTex,
+            memcpy((u8*)msgCtx->textboxSegment + MESSAGE_STATIC_TEX_SIZE + 0x900, gRedMessageXRightTex,
                    strlen(gRedMessageXRightTex) + 1);
             numLines = 2;
             msgCtx->msgBufPos += 2;
@@ -2636,9 +2636,9 @@ void Message_Decode(PlayState* play) {
             msgCtx->textboxBackgroundYOffsetIdx = (font->msgBuf[msgCtx->msgBufPos + 3] & 0xF0) >> 4;
             msgCtx->textboxBackgroundUnkArg = font->msgBuf[msgCtx->msgBufPos + 3] & 0xF;
 
-            memcpy((uintptr_t)msgCtx->textboxSegment + MESSAGE_STATIC_TEX_SIZE, gRedMessageXLeftTex,
+            memcpy((u8*)msgCtx->textboxSegment + MESSAGE_STATIC_TEX_SIZE, gRedMessageXLeftTex,
                    strlen(gRedMessageXLeftTex) + 1);
-            memcpy((uintptr_t)msgCtx->textboxSegment + MESSAGE_STATIC_TEX_SIZE + 0x900, gRedMessageXRightTex,
+            memcpy((u8*)msgCtx->textboxSegment + MESSAGE_STATIC_TEX_SIZE + 0x900, gRedMessageXRightTex,
                    strlen(gRedMessageXRightTex) + 1);
 
             msgCtx->msgBufPos += 3;
diff --git a/soh/src/code/z_skelanime.c b/soh/src/code/z_skelanime.c
index 3449c2e..f1d5386 100644
--- a/soh/src/code/z_skelanime.c
+++ b/soh/src/code/z_skelanime.c
@@ -906,7 +906,7 @@ void AnimationContext_SetLoadFrame(PlayState* play, LinkAnimationHeader* animati
         if (animData == NULL) {
             return;
         }
-        memcpy(ram, (uintptr_t)animData + (((sizeof(Vec3s) * limbCount + 2) * frame)), sizeof(Vec3s) * limbCount + 2);
+        memcpy(ram, (const u8*)animData + (((sizeof(Vec3s) * limbCount + 2) * frame)), sizeof(Vec3s) * limbCount + 2);
     }
 }
 
